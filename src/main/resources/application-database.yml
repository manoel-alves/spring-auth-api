spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/auth_api_db
        username: postgres
        password: postgres
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true